---
import '../styles/tokens.css';
import Sidebar from '../components/admin/Sidebar.astro';

interface Props {
  title: string;
  active: 'listings' | 'visits' | 'tenants' | 'contracts' | 'metrics' | 'leads';
  brokerName?: string;
}
const { title, active, brokerName } = Astro.props;
---

<!doctype html>
<html lang="es-MX" data-theme="light">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{title} · EZRA Admin</title>
  <link rel="icon" type="image/jpeg" href="/assets/ezra-icon.jpg" />
</head>
<body>
  <div class="ezra-admin-shell">
    <Sidebar active={active} brokerName={brokerName} />
    <main class="ezra-admin-main">
      <slot />
    </main>
  </div>
</body>
</html>

<style>
  .ezra-admin-shell {
    display: flex;
    min-height: 100vh;
    background: var(--ezra-bg);
  }
  .ezra-admin-main {
    flex: 1;
    min-width: 0;
  }
</style>
