# Estas variables se inyectan automáticamente por wrangler en Cloudflare.
# Para desarrollo local, copia este archivo a `.dev.vars` (NO `.env`).

ADMIN_PASSWORD=cambia-esto-en-produccion
SESSION_SECRET=genera-uno-largo-y-aleatorio
RESEND_API_KEY=
